# FastAPI and Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# AI/ML Libraries
openai==1.60.2
langchain==0.3.7
langchain-community==0.3.7
langchain-openai==0.2.6
chromadb==0.5.11
sentence-transformers==3.2.1

# Database and ORM
sqlmodel==0.0.22
sqlalchemy==2.0.35
aiosqlite==0.20.0

# Data Validation and Settings
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
email-validator==2.1.1

# Authentication and Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Utilities
tiktoken==0.7.0
httpx==0.27.2
rich==13.9.2
python-multipart==0.0.12
markdown==3.10

# Backend A: STT (Speech-to-Text)
openai-whisper==20231117
pydub==0.25.1

# Backend A: PDF Processing (Multimodal - Text and Images)
pymupdf==1.24.0
Pillow==10.4.0

# Korean Spell Checker (Optional - Excluded due to build issues)
# py-hanspell==1.1
# Note: py-hanspell is excluded from requirements.txt due to build errors.
#       Install manually if needed. Code handles ImportError so it works without it.

# Notes:
# - langchain-openai 0.2.6 requires openai>=1.54.0,<2.0.0
# - ffmpeg must be installed separately on the system (for Whisper STT)
